(function( window, Abacus ) {
  Abacus.event = {
    bind: function( name, callback, context ) {
    },
    unbind: function( name, callback, callback) {
    },
    trigger: function(eventName) {
    }
  };
})( this, this.Abacus );